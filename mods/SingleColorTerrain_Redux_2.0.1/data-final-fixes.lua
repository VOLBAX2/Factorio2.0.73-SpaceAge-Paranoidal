--require("prototypes.decoratives")

-- local message = ""
-- for _, proto in pairs(data.raw["optimized-decorative"]) do
    -- message = message .. 'data.raw["optimized-decorative"]["' .. proto.name .. '"] = nil\n'
-- end
-- log(message)